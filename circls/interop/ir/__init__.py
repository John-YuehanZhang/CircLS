"""Front-end-agnostic interop layer: the PauliCircuit IR contract and
the PBC -> PPM transformation chain (Y elimination, gadget expansion,
PPM import).  Every front-end translates into this layer."""
